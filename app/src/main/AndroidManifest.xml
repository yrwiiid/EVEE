<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evee">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:theme="@style/Theme.EVEE"
        android:allowBackup="true"
        android:label="EVEE"
        android:icon="@drawable/logo_red"
        android:roundIcon="@drawable/logo_red"
        android:supportsRtl="true"
        >

        <!-- Main Activity -->
        <activity android:name=".dashboard" />

        <activity android:name=".MenstruasiActivity" />

        <activity android:name=".SavedArtikelActivity" />


        <!-- login activity -->
        <activity android:name=".loginactivity"/>

        <!-- Register Activity -->
        <activity android:name=".registeractivity" />

        <!-- lupa password -->
        <activity android:name=".ForgotPasswordActivity" />

        <!-- Login Activity: entry point -->
        <activity
            android:name=".splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
